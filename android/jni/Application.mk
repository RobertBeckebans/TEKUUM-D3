#APP_MODULES      := tekuumjni #stlport_shared
APP_STL           := gabi++_static #stlport_static
#APP_PROJECT_PATH := $(call my-dir)/..
#APP_CPPFLAGS      += -frtti 
#APP_CPPFLAGS      += -fexceptions
APP_ABI           := armeabi-v7a # armeabi-v7a
NDK_TOOLCHAIN_VERSION := clang


